const add = (a, b) => a + b;
const sub = (a, b) => a - b;
const multi = (a, b) => a * b;
const div = (a, b) => a / b;

//exporting all the above function by as one object
module.exports = { add, sub, multi, div };
